import type {
	IExecuteFunctions,
	INodeExecutionData,
	IDataObject,
	INodeProperties,
	IDisplayOptions,
} from 'n8n-workflow';
import { ApiClient } from '../../../../transport/ApiClient';

/**
 * List VMware Cloud Director reference regions.
 *
 * HTTP method: GET
 * Endpoint: /v2/vmwareCloudDirector/reference/region
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function description(displayOptions: IDisplayOptions): INodeProperties[] {
	return [];
}

/**
 * Executes the List VMware Cloud Director Reference Regions operation.
 */
export async function execute(this: IExecuteFunctions): Promise<INodeExecutionData[]> {
	const client = new ApiClient(this);
	const data = (await client.httpGet('/v2/vmwareCloudDirector/reference/region')) as IDataObject[];
	return this.helpers.returnJsonArray(data);
}
